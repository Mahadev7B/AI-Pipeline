# DECISIONS.md — Decision Log

Append-only. Never silently reverse a prior decision — if an agent believes
an existing decision should change, it proposes a **new** decision entry
and runs it through review; the old entry stays as-is for history.

From Phase 1 onward, this file is the git-readable mirror of the `decisions`
table in the operational SQLite database (see `DATA_MODEL.md`) — SQLite is
the writable source of truth, this file is the durable, diffable export.

## Format

```
### DEC-<NNN> — <short title>
Date: <YYYY-MM-DD>
Problem: <what needed deciding>
Options considered: <list>
Decision: <what was chosen>
Reason: <why>
Tradeoffs: <what was given up>
Agent recommending it: <agent name>
Founder approval: <required/not required — and outcome if resolved>
```

## Log

### DEC-001 — Control Center mockup direction (recommended, pending Founder approval)
Date: 2026-08-28
Problem: Which of three Control Center visual/structural directions should Phase 2 build from?
Options considered: Variant A (Pipeline First), Variant B (Agent First), Variant C (Command Center) — see `MOCKUP_CRITIQUE.md` for full critique of each.
Decision: Recommend Variant C (Command Center) as the landing "Overview" screen, with Variant A's pipeline design as the dedicated "Pipeline" tab and Variant B's agent roster/capability view as the dedicated "Agents" tab — reconciling all three rather than discarding two.
Reason: The Founder's stated priority is minimizing interruptions to founder-only decisions; Command Center is the only variant that puts Founder Inbox, Risks, and Executive Discussion front-and-center on first open, while still preserving A's and B's strengths as deeper views.
Tradeoffs: Command Center is the busiest single screen of the three; agent capability detail is a drill-in rather than visible on the landing view.
Agent recommending it: Design Agent (critique), Orchestrator (log entry).
Founder approval: **APPROVED 2026-08-28.** Command Center = Overview, Pipeline First = Pipeline tab, Agent First = Agents tab.

### DEC-002 — Control Center visual style: dark vs. light (recommended, pending Founder approval)
Date: 2026-08-28
Problem: Which visual treatment should the approved Command Center structure use — the existing dark "operating console" direction, or a new lighter/premium alternative?
Options considered: Style A — refined dark (`Main.dc.html`); Style B — lighter premium (`OverviewLight.dc.html`) — see `MOCKUP_CRITIQUE.md`, "Visual style" for full critique.
Decision: Recommend Style A (refined dark) as the primary direction; keep Style B on file as a considered alternative rather than discarding it.
Reason: The product's core value is watching AI agents work in real time — a console aesthetic communicates that more directly than a document-like light theme, which reads calmer but less "live."
Tradeoffs: A risks tipping into "ops-tool" territory if not kept disciplined; B is easier on the eyes for long sessions but undersells that a live system is running underneath.
Agent recommending it: Design Agent.
Founder approval: **APPROVED 2026-08-28.** Refined dark (Style A) is the default visual direction; Style B (light) is retained as an optional future theme, not built in Phase 2 unless separately requested.

### DEC-003 — Phase 0 final approval
Date: 2026-08-28
Problem: Whether the Phase 0 architecture proposal (14-agent operating model, workflow, templates, Skill/Model Registry, Executive Meetings) and the Control Center design direction (DEC-001, DEC-002) are approved to proceed into Phase 1 — Foundation.
Options considered: Approve as-is; request further iteration; reject and restart.
Decision: **APPROVED.** Phase 0 is complete. Phase 1 — Foundation begins, scoped to: instantiating the SQLite operational database (per the data-model clarifications below), wiring the 14 agent definitions as real Claude Code subagents, executing the TASK-001 sample walkthrough, and generating real status reporting. Phase 2 (Control Center UI) remains explicitly out of scope until Phase 1 is reviewed and separately approved.
Reason: Two rounds of mockup refinement (v2, v2.1) resolved every open concern the Founder raised; the remaining data-model clarifications (agent_runs, risks, task_steps, projects) are architecture-level fixes, not design changes.
Tradeoffs: None — this is a gate opening, not a scope tradeoff.
Agent recommending it: Orchestrator.
Founder approval: **APPROVED 2026-08-28.**

### DEC-004 — Phase 1 Foundation complete, two known gaps disclosed
Date: 2026-08-28
Problem: Whether Phase 1 (SQLite operational database, 14 wired subagents, TASK-001 walkthrough, real status reporting) is complete and ready for Founder review before Phase 2 begins.
Options considered: Declare complete with gaps disclosed; hold Phase 1 open until gaps are closed.
Decision: Declare Phase 1 **complete**, with two gaps explicitly disclosed rather than silently accepted: (1) `approval-decide` has no real identity check — a `--confirm-founder-decision` flag makes it a deliberate act but not enforced authentication; (2) subagent `Bash` tool grants are not scoped below the tool-category level, so a subagent's actual shell access is broader than its documented Permissions section implies. Neither blocks Phase 1's stated goal (a working, auditable, deterministic operational core); both require an identity/permission layer that is Phase 2/3 (Control Center) scope.
Reason: Holding Phase 1 open until these are closed would mean waiting on infrastructure (real user auth, finer-grained tool scoping) that doesn't exist yet and wasn't asked for in Phase 1 — better to ship a disclosed, honest state than delay for an unscoped fix.
Tradeoffs: The Founder is trusting procedural/documented enforcement (agent role docs, CLI speed bumps) over technical enforcement for these two specific actions until Phase 2 or later.
Agent recommending it: Security (see `ops/reviews/security-phase1.md`), Orchestrator.
Founder approval: **APPROVED 2026-08-28. Phase 1 — Foundation is formally closed.** Recorded as decision id=2 in the operational database (`decisions` table), linked to approval id=3 — see `ops/db/opsdb.py query "SELECT * FROM decisions WHERE id=2"`. The two disclosed gaps are not resolved by this approval — they carry forward as tracked risks (company-scoped, `risks` table) into Phase 2, not silently dropped.

### DEC-005 — Human-facing role rename: Orchestrator → Chief of Staff
Date: 2026-08-30
Problem: Founder directive: rename the human-facing company role currently called "Orchestrator" to "Chief of Staff" — a role-name clarification only, explicitly not a change to authority, permissions, or responsibilities, and explicitly not a second/duplicate agent. Exactly one operating coordination agent must remain.
Options considered: Rename the DB `agents.name` value and every stored TEXT reference across ~10 tables (rejected — unnecessary migration risk, would also rewrite history); rename `.claude/agents/orchestrator.md`'s filename and `subagent_type` (rejected — would break every invocation across this whole operating system and risks reading as two agents, exactly what the Founder forbade); add a single Founder-facing `display_name()` mapping layer applied only at render sites, leaving the internal machine identity `orchestrator` unchanged everywhere (adopted).
Decision: Introduced `ops/db/derived_state.py`'s `display_name()`, a one-entry mapping (`{"orchestrator": "Chief of Staff"}`, default fallback the key itself) applied only at Founder-facing render sites — agent roster/detail, pipeline task-owner, Overview, Founder Inbox, Decisions byline, Executive Meeting validation note, `CURRENT_STATUS.md`'s Agents section. Internal machine identity, the `agents` table row, `.claude/agents/orchestrator.md`'s filename and `subagent_type`, and every historical record (this file, `ops/reviews/*.md`, stored message/decision/review body text, the approved Phase 0 mockups) remain `orchestrator`/"Orchestrator" unchanged, permanently.
Reason: Smallest safe change per the Founder's explicit instruction to prefer preserving the stable machine identity and avoid unnecessary database/runtime migration, while giving every current and future Founder-facing render site a single, consistent source of truth for the display label.
Tradeoffs: Historical text (old reviews, this file's own DEC-001–004 above, stored message bodies) correctly keeps reading "Orchestrator" forever — an intended, disclosed outcome (a record reflects the name that existed when it was created), not a bug or an incomplete rename. The other 13 agents' identical raw-machine-key rendering is unchanged — out of scope, not incidentally fixed.
Agent recommending it: CTO (see `ops/reviews/cto-chief-of-staff-rename.md`, Red Team rounds at `ops/reviews/red-team-chief-of-staff-rename.md`), Chief of Staff/Orchestrator.
Founder approval: **APPROVED 2026-08-30 (Founder directive, Part A).** TASK-012, all gates PASS/CONFORMS — see `ops/reviews/code-review-chief-of-staff-rename.md`, `ops/reviews/qa-chief-of-staff-rename.md`, `ops/reviews/security-chief-of-staff-rename.md`, `ops/reviews/cto-chief-of-staff-rename-conformance.md`. Recorded as decision id=8 in the operational database.

### DEC-006 — Milestone 2B4: Founder authentication mechanism selection
Date: 2026-08-30
Problem: The session-token mechanism (Milestone 2B1 onward) proves a request came from a page this server process rendered, but does not prove a human, specifically the Founder, sent it — `risks.id=2`, open since Phase 1. This milestone had to materially close that gap for the current local/internal, solo-Founder, loopback-only Control Center deployment, without adding a paid identity provider, external cloud infrastructure, or any third-party dependency unless the threat model genuinely required it (Security and Red Team both confirmed it did not).
Options considered: Auth0/Clerk/Firebase/OAuth/enterprise IAM (rejected — disproportionate cost/dependency for a single local trusted operator's own machine; would require separate Founder approval for external cost/dependency anyway); an environment-variable passphrase (rejected — leaks via shell history and `/proc/<pid>/environ`, worse exposure than the alternative, and forces re-entry every process start with no session UX); a single Founder passphrase, `hashlib.scrypt`-hashed, credential stored outside git in a 0600 file bootstrapped via a new stdlib-only CLI, gated by a server-side session cookie layered onto the existing centralized `do_POST()`/`do_GET()` check (adopted).
Decision: Implemented `ops/control-center/founder_auth.py` (`scrypt` N=2**17/r=8/p=1, 16+ character passphrase floor, atomic 0600 credential file outside git, never touching `operations.sqlite3`) plus a Founder-session cookie (`HttpOnly`, `SameSite=Strict`, 30-minute idle / 12-hour absolute timeout, in-memory only, wiped on restart) gating all 9 write/auth routes and every GET read at the same centralized location the pre-existing CSRF `SESSION_TOKEN` check already occupied — kept, not replaced. Fully serialized `/api/login` verification closes both the stated brute-force attempt cap and a concurrent-`scrypt` memory-exhaustion DoS, independently verified three separate times under adversarial conditions (Code Review's 60-simultaneous reproduction, Security's 45,820-request/40-second sustained flood) — all PASS.
Reason: The smallest credible mechanism for the actual deployment model — a solo Founder's own local internal tool, not a public SaaS product — that genuinely answers "does the entity submitting this request know a secret only the Founder was ever told," backed by a real, independently-reproduced technical guarantee rather than a documented convention.
Tradeoffs: Does not and structurally cannot defend against an agent with Bash tool access sharing the Founder's own OS-user filesystem/process principal (`risks.id=3`, explicitly out of scope this milestone) — such an agent can read or overwrite the credential file directly, run `founder_auth.py` itself, or `PTRACE_ATTACH` the running server process and read session state out of memory. Also disclosed: the shared, non-identity-scoped lockout can itself be flooded by an actor already inside this design's own threat class to deny the Founder's own genuine logins (Red Team's Milestone 2B4 finding F1) — no cheap in-scope fix exists without touching `risks.id=3`. Neither gap is hidden; `risks.id=2` moves to `mitigated`, deliberately not `resolved`.
Agent recommending it: CTO (see `ops/reviews/cto-milestone2b4-architecture.md`), Security threat model (`ops/reviews/security-milestone2b4-threat-model.md`), Red Team (`ops/reviews/red-team-milestone2b4-architecture.md`).
Founder approval: **APPROVED 2026-08-30 (Founder directive, Part B — Milestone 2B4).** TASK-013, all gates PASS/CONFORMS — see `ops/reviews/code-review-milestone2b4.md`, `ops/reviews/qa-milestone2b4.md`, `ops/reviews/security-adversarial-milestone2b4.md`, `ops/reviews/cto-milestone2b4-conformance.md`. Recorded as decision id=9 in the operational database. `risks.id=2` moved `open` → `mitigated` — see `ops/db/opsdb.py query "SELECT * FROM risks WHERE id=2"`.

### DEC-007 — Phase 3A: zero-tool automated Code Review + in-process background poller, not native tool grants or a second process
Date: 2026-08-31
Problem: The Founder authorized Phase 3A — a Chief of Staff Founder conversational interface, plus one narrowly-scoped autonomous handoff (Developer complete → Code Review) — explicitly requiring the system to "recognize completion automatically" and route a completed Developer handoff to Code Review without a human click, while explicitly refusing to authorize full Phase 3 automation, refusing to claim `risks.id=3` (Bash tool access cannot be scoped below the tool-category level) was solved, and requiring the Security threat model to specifically examine how autonomous operation changes that risk's practical consequence.
Options considered: Real Bash/filesystem tool grants for an unsupervised automated Code Review invocation, so it could genuinely explore the repository the way a human-supervised review session can (rejected — would be the first unattended-Bash precedent in this system's history, under the identical already-open, already-flagged OS-user principal `risks.id=3` names); a standalone second process/script polling the database for eligible tasks (rejected — adds an operational surface the Founder would have to separately remember to run, monitor, and stop, contradicting this system's deliberate one-process design); zero-tool invocation (`--tools ""`, `--strict-mcp-config`, unconditional — identical to every invocation this system has ever made) with deterministic Python assembling a real transcript — task metadata, the Developer's handoff record, a real `git diff` between validated commit SHAs, full file content retrieved via `git show <sha>:<path>` (the git object database, never a live filesystem read), and `CODING_STANDARDS.md` — plus an in-process `daemon=True` polling thread living inside `server.py`'s own existing process (adopted).
Decision: Implemented exactly as adopted. `AUTOMATED_REVIEW_ALLOWLIST` invokes `code-review` with the same unconditional zero-tool flags every other invocation in this codebase has always used — verified to have zero diff across `agent_runtime.py`'s `_run_claude()` for this entire milestone, independently re-confirmed by Code Review, Security's adversarial review, and CTO's post-implementation conformance. `automation.py`'s poll loop (`POLL_INTERVAL_S=20`) lives inside `server.py`'s existing process, started/stopped alongside it, gated by a default-disabled kill switch (`automation_state.enabled`) only the Founder can flip via two new CSRF+session-gated routes. New `automation_events` table's `UNIQUE` constraint on the triggering `task_status_history` row is the real, DB-enforced idempotency guarantee (confirmed under 20 real concurrent claim attempts — exactly one succeeded). A `PASS` leaves `tasks.status` unchanged at `CODE_REVIEW` (no automatic advance to `QA`); a `REJECT` performs a single mechanical `CODE_REVIEW → IN_DEVELOPMENT` status rollback, never a new Developer invocation — both per the Founder's explicit, narrow authorization. Part A of the same milestone shipped the Chief of Staff's own real conversational interface (`POST /api/chief-of-staff/ask`, the first-ever real `claude --agent orchestrator` invocation in this system's history, also zero-tool, also gated by the same auth boundary), reusing the existing Executive Meeting machinery for "ask other agents what they think" rather than inventing a second orchestration system.
Reason: Every existing invocation in this codebase has been deliberately, consistently restricted to zero tools specifically because no human is watching a given call in real time — that reasoning applies with equal or greater force to an invocation not even triggered by a Founder action, but by a background poller noticing database state on its own. A single existing process, extended with a daemon thread, is the smallest addition that satisfies "the system recognizes completion automatically" without adding a second operational surface the Founder would have to separately manage.
Tradeoffs: Does not resolve, narrow, or reduce `risks.id=3` — its practical consequence genuinely increases in two independently additive ways, disclosed in full rather than hidden: (1) a same-OS-user actor no longer needs to forge an authenticated HTTP request to get a real, costed model invocation to run — writing a plausible `CODE_REVIEW` transition and handoff via `opsdb.py` directly (already possible before this milestone) is now sufficient, since the poller acts unattended on its own cycle; (2) the Python code assembling diff/file content now walks real filesystem paths and shells out to `git` based on database content the same actor already controls — mitigated (path-traversal validation including symlink resolution, commit-SHA format/existence validation, fixed argv with `--` separators, git-object-database content retrieval) but not eliminated as a new surface. Automated Code Review structurally cannot catch cross-file consistency/duplication defects a human-supervised session could (disclosed in the automated mode's own persona note). Both gaps were adversarially re-verified, not merely asserted: Security's adversarial review reproduced the full same-OS-user attack end-to-end and confirmed its blast radius is exactly as bounded as documented (one zero-tool invocation, ~$0.08 real cost), and ran live prompt-injection attempts against both new invocation types — both were explicitly detected and refused by the model itself.
Agent recommending it: CTO (see `ops/reviews/cto-phase3a-architecture.md`), Security threat model (`ops/reviews/security-phase3a-threat-model.md`), Red Team (`ops/reviews/red-team-phase3a-architecture.md`).
Founder approval: **APPROVED 2026-08-31 (Founder directive, "PHASE 3A: LIMITED AUTOMATION + CHIEF OF STAFF FOUNDER INTERFACE").** TASK-015, all gates PASS/CONFORMS across two Development passes (Part A, then Part B, per Red Team's recommendation) — see `ops/reviews/code-review-phase3a-parta.md`, `ops/reviews/code-review-phase3a-partb.md`, `ops/reviews/qa-phase3a.md` (the Founder's own 12-point acceptance test demonstrated end-to-end with real, unmocked model invocations), `ops/reviews/security-adversarial-phase3a.md`, `ops/reviews/cto-phase3a-conformance.md`. Recorded as decision id=10 in the operational database. `risks.id=3` remains `open`, `mitigation` text updated to reflect the two-part consequence increase — see `ops/db/opsdb.py query "SELECT * FROM risks WHERE id=3"`. Full Phase 3 automation (automatic Code Review PASS → QA, QA → Security, Security → Release, and production deployment) remains explicitly unauthorized.

### DEC-008 — Pause TASK-017 security hardening mid-Development; prioritize product architecture completion
Date: 2026-08-31
Problem: TASK-017 (the `risks.id=3` reduction milestone the Founder had separately authorized) had gone through three real adversarial review rounds — Security returned one CONCERNS verdict, fixed; Red Team returned two REJECT verdicts, both fixed and independently re-verified, with a final PASS — before Development began implementing the approved design. The Founder judged that continuing to spend repeated architecture/Red Team correction cycles perfecting this one security mechanism, before the rest of the product architecture (remaining Phase 3 orchestration, a Founder progress view, a defined Founder Test Readiness milestone) is complete enough for the Founder to actually test the product, was the wrong sequencing right now.
Options considered: Continue TASK-017 to completion before any other work (rejected — delays Founder testability with no corresponding safety requirement, since this is a pre-production, single-operator, internal system); abandon TASK-017 and mark `risks.id=3` resolved or drop it silently (rejected — explicitly against the Founder's own instruction; the risk is not solved and must never be described as such); pause TASK-017 mid-Development, preserve all findings and the approved architecture, explicitly record it as deferred rather than resolved, redirect company attention to product-architecture completion, and define an explicit checkpoint for when security hardening resumes (adopted).
Decision: TASK-017 is paused mid-Development by Founder direction. Its findings — the full TASK-016 investigation trail and the three-round-reviewed TASK-017 architecture (`ops/reviews/cto-risk3-milestone-architecture.md`, `security-risk3-milestone-threat-model.md`, `red-team-risk3-milestone-review.md`) — are preserved, not discarded. `risks.id=3` remains `open`; its `mitigation` text was updated to record the pause and point to the preserved findings, not to claim resolution. Company attention shifts to: (1) a CTO + Chief of Staff review of what remains before a complete, Founder-testable product architecture (TASK-018); (2) design, not implementation, of a Founder Work Progress capability sourced from the existing operational database — no second project-management system; (3) a concrete Founder Test Readiness definition naming exactly which of the Founder's 13 requested testing capabilities already work and which don't; (4) this file's corrected phase count plus a proposed, unapproved Phase 4 (Human AI Team Experience — avatars/voice), positioned for later Founder approval only, not started.
Reason: This is a prioritization decision by the Founder, not a technical judgment that the risk is acceptable or solved — the Founder was explicit that security work returns before any broader unattended automation, external users, production credentials, production deployment automation, or multi-user access. Continuing an open-ended architecture-correction loop on one security mechanism, while the Founder still cannot see overall task progress or exercise the product end-to-end, was judged to provide less value right now than finishing the architecture the Founder needs to actually operate and test the company. Role-level and task-level least-privilege access remains the target architecture for when security work resumes — this decision does not change that target, only its sequencing.
Tradeoffs: `risks.id=3`'s practical consequence, as disclosed since Phase 3A, remains unreduced — the approved TASK-017 design that would have started closing it is not yet built. This is an accepted, explicitly time-boxed tradeoff (a named checkpoint before higher-risk operation), not an indefinite deferral — see `risks.id=3`'s updated mitigation text and this file's TASK-017 note (Phase 3 section) for the explicit resume condition.
Agent recommending it: Founder-directed; Chief of Staff and CTO tasked with the resulting product-architecture completion review (TASK-018).
Founder approval: **This decision IS the Founder's own directive** ("FOUNDER DIRECTIVE — PAUSE SECURITY HARDENING, FINISH PRODUCT ARCHITECTURE FIRST", 2026-08-31) — no separate approval step applies. Recorded as decision id=11 in the operational database. `risks.id=3` remains `open`, deliberately not `mitigated` or `resolved` — see `ops/db/opsdb.py query "SELECT * FROM risks WHERE id=3"`.

### DEC-009 — Founder UI Completeness: four-milestone plan (A-D), two distinct readiness definitions
Date: 2026-08-31
Problem: Two prior TASK-018 rounds under-scoped what the Founder actually required. Round 1 proposed two milestones; the Founder corrected that "necessary but not sufficient" and required 100% feature-complete via a strict product-UI-reachability bar (if the Founder has to open GitHub, SQLite, Markdown, terminal output, or an outside AI to understand what the company is doing, the UI is not feature-complete). Round 2's CTO audit (v2) found the true gap was larger than round 1 assumed — Active Work dashboard, Task Detail page, Risks register, and Project/Phase Progress were all found MISSING — and proposed three milestones with Project/Phase Progress deferred indefinitely as lower priority. The Founder corrected this a second time: Project/Phase Progress must be part of the definition of "100% complete," not permanently deferred, though exploratory testing may begin before it ships. Separately, CTO's v2 document's own prose "headline count" (16 COMPLETE/7 PARTIAL/7 MISSING) did not match its own per-item audit table — a direct recount against that table found 18 COMPLETE/6 PARTIAL/6 MISSING of 30 total — and the Founder required one authoritative, reconciled count before any percentage was used again.
Options considered: Two milestones (round 1, rejected by the Founder as insufficient); three milestones with Project/Phase Progress deferred indefinitely (round 2, rejected by the Founder as narrowing the definition of "complete" after the fact); four milestones with two explicitly separate readiness definitions — A+B+C for exploratory testing readiness, A+B+C+D for 100% feature-complete (adopted).
Decision: Official plan is Milestone A (Active Work dashboard + Task Detail page, shipped together since they share one computed progress model and a dashboard linking to a nonexistent detail page would be a broken experience), Milestone B (company-wide AI cost visibility across all four invocation paths — Ask-Agent, Executive Meetings, Chief of Staff, automated Code Review), Milestone C (company-wide Risks register — `risks.id=3` itself currently has no Founder-facing page anywhere), Milestone D (Project/Phase Progress on real structured data, explicitly no invented percentages — real fractions where genuinely queryable, otherwise an explicit status). Build order A → B → C → D unless CTO finds a concrete dependency requiring otherwise. Two distinct, permanently separate readiness states, kept visible everywhere this project reports status: **Exploratory Founder Testing ready** = A+B+C complete (the Founder may begin testing a real app idea at this point, if the Chief of Staff recommends it); **Founder UI 100% feature-complete** = A+B+C+D complete — A+B+C being done does not itself mean the UI is 100% complete. Authoritative UI audit: 30 capabilities audited, 18 COMPLETE, 6 PARTIAL, 6 MISSING — reconciled by direct recount against `ops/reviews/cto-product-architecture-completion-v2.md`'s own per-item table, correcting that document's inconsistent prose summary. Security hardening (TASK-017/`risks.id=3`) and the remaining Phase 3 orchestration automation stay explicitly deferred, not bundled into this work, unless a specific UI milestone is found to introduce a genuinely new material vulnerability. Gates per milestone: CTO architecture → Design review where Founder-facing UX changes → Red Team → Development → Code Review → QA → a focused Security review scoped to newly introduced risk only → CTO final conformance — proportionate, not open-ended: only a concrete blocking defect repeatedly halts progress; lower-severity findings become disclosed follow-up work, not grounds for another correction loop.
Reason: The Founder's own bar (100% feature-complete meaning everything reachable through the actual product UI, no exceptions carved out after the fact) requires all four gaps closed, not three — deferring Project/Phase Progress indefinitely would have quietly redefined "complete" to mean something smaller than what was actually asked for. Separating exploratory-testing-readiness from full completeness lets real Founder testing start without waiting for all four milestones, independently honoring the Founder's separate, standing instruction not to postpone testing until every feature is done.
Tradeoffs: Milestone D requires genuinely new schema work (no phase/milestone concept exists in the database today), not just a new page — real, disclosed net-new scope, not a relabeling of existing data, accepted as necessary rather than narrowed away. The three-round back-and-forth to reach this plan (two Founder corrections, one internal counting error caught and fixed) is itself now the documented record of how the scope was actually settled, preserved rather than smoothed over in retrospect.
Agent recommending it: CTO (`ops/reviews/cto-product-architecture-completion.md`, `cto-product-architecture-completion-v2.md`), Chief of Staff (`ops/reviews/chief-of-staff-product-architecture-synthesis.md`, `chief-of-staff-product-architecture-synthesis-v2.md`).
Founder approval: **APPROVED 2026-08-31 (Founder directive, "FOUNDER CORRECTION — FINALIZE UI COMPLETENESS PLAN"), with the Founder's explicit instruction not to wait for a further approval round given the underlying intent (finish the Founder cockpit first) was already approved.** Recorded as decision id=12 in the operational database. Milestone A begins immediately following this decision — see TASK-019.

### DEC-010 — Sequencing: resume TASK-017 security hardening before building full Phase 3 automation
Date: 2026-09-01
Problem: With Milestone A (Task Detail + Active Work) shipped and Milestone B (cost visibility) in review, the Founder asked to "finish Phase 3 completely" — automate the remaining handoffs (Code Review PASS → QA, QA → Security, Security → Release, automated Developer reinvocation on reject). This is exactly the "broader unattended automation" trigger the Founder's own earlier directive (DEC-008, "PAUSE SECURITY HARDENING, FINISH PRODUCT ARCHITECTURE FIRST") named as the condition for resuming TASK-017/`risks.id=3` hardening *before*, not after, such automation ships. Proceeding straight to full Phase 3 automation while `risks.id=3` stays open and TASK-017's own design (Development-complete since before the pause) remains unreviewed would have silently contradicted the Founder's own standing sequencing rule.
Options considered: Build full Phase 3 automation now, treating the earlier trigger as superseded by the new directive (rejected — would silently override a directive the Founder never explicitly revisited); build full Phase 3 automation now with an explicit, documented risk-acceptance overriding the trigger (offered, not chosen); a narrower Phase 3 (some handoffs automated, one human checkpoint kept) without resuming TASK-017 first (offered, not chosen); resume TASK-017 to completion — Code Review → QA → Security-adversarial → CTO conformance on its already-complete Development — before starting any Phase 3 automation work (adopted, the Founder's explicit choice when presented with the direct conflict via `AskUserQuestion`).
Decision: TASK-017 resumes at exactly the gate it was paused at — Development was already complete (commit `fdaf253`) when DEC-008 paused it; it now proceeds through Code Review, QA, Security-adversarial review, and CTO conformance, the same full gate sequence as every other milestone. Only once TASK-017 reaches a final disposition (`risks.id=3` moved to `mitigated`-with-disclosed-residual-scope, or another Founder-reviewed outcome) does Phase 3 automation work begin. `risks.id=4` (the `reviewer_invocations` table specified but never created in the live database, discovered during Milestone B's Red Team review) is exactly the kind of gap this resumed review sequence exists to catch — expected to surface as a real Code Review finding, not pre-fixed outside the process.
Reason: The Founder chose this explicitly, as the recommended option, when the direct conflict was surfaced rather than silently resolved either way: build on a reviewed security foundation before adding more unattended automation on top of an already-open, medium-severity risk, rather than accepting new automation risk while an already-scoped mitigation sits unreviewed.
Tradeoffs: Phase 3 completion is delayed by however long TASK-017's remaining gates take (Code Review, QA, Security-adversarial, CTO conformance — likely several review rounds, based on this project's own track record on similarly-sized milestones, e.g. Milestone A's seven rounds). This is an accepted, explicit tradeoff, not a silent one — the Founder chose sequencing correctness over speed when given the direct choice.
Agent recommending it: Founder-directed, surfaced via `AskUserQuestion` rather than resolved unilaterally in either direction.
Founder approval: **This decision IS the Founder's own choice**, selected from the presented options 2026-09-01. Recorded as decision id=13 in the operational database. TASK-017 moves `BLOCKED` → `CODE_REVIEW` immediately following this decision.

### DEC-011 — TASK-017/risks.id=3: pursue OS-level sandboxing over the narrowed Developer-denylist protection
Date: 2026-09-01
Problem: TASK-017's hook-based Developer denylist was found (QA) to never fire in the deployed non-interactive context; CTO's follow-up investigation found the fix restores it only against non-adversarial mistakes — a genuinely adversarial or compromised Developer session can revert the workspace-trust flag itself via plain Bash, zero audit trail, because every role shares one OS user with unrestricted Bash. The Chief of Staff synthesis (`ops/reviews/chief-of-staff-task17-hook-gap-synthesis.md`) asked the Founder to choose: accept that narrowed, non-adversarial-only protection as sufficient for now, or have CTO scope real OS-level/process-separation sandboxing, which CTO's original investigation (`ops/reviews/cto-risk3-architecture-investigation.md` §3.6/3.7) had already evaluated and named as the one option that closes the gap durably, but had not recommended as a first step given this risk's medium severity and single-operator scope.
Options considered: Accept the narrowed Developer-denylist protection (ship the trust-flag fix, two secondary bug fixes, and a self-checking monitor; `risks.id=3` stays open, honestly labeled as closed only for reviewer roles) — offered, not chosen; scope and build real OS-level sandboxing for Developer (and other roles as warranted) — per-role OS user separation or bubblewrap/namespace sandboxing, per §3.6/3.7 — closing the adversarial gap the hook-based approach cannot (adopted, the Founder's explicit choice after a full walkthrough of both options' cost and residual risk).
Decision: CTO will scope real OS-level/process-separation sandboxing for the Developer role (and any other role the scoping finds warrants it), building on the two candidate shapes already named in §3.6 (per-role OS user separation) and §3.7 (bubblewrap/namespace sandboxing). This is a new architecture cycle, not a continuation of TASK-017's already-implemented hook-based design — opened as **TASK-023**, starting at CTO architecture. The Founder explicitly required Red Team review as a non-skippable gate on this milestone, called out here at the Founder's own request (already standard practice in this project's gate sequence). TASK-017 moves to `BLOCKED`, not abandoned: its real, delivered work (the reviewer zero-tool rollout for Code Review/Security/Red Team) stands unchanged; its own remaining cheap fixes (trust-flag fix, two secondary Bash-string bugs, a self-checking monitor) are neither shipped nor discarded by this decision — TASK-023's CTO architecture scoping must state explicitly whether they fold into its Development pass or ship standalone.
Reason: The Founder judged the residual adversarial gap unacceptable now that its true ceiling is known, and chose to close it durably rather than ship a narrower, honestly-labeled interim fix. This is the Founder's own tradeoff to make, per CTO's investigation naming it explicitly as a Founder-level call, not a technical judgment CTO or any reviewer could resolve unilaterally.
Tradeoffs: Real cost, disclosed directly to the Founder before the decision: CTO's own investigation named per-role OS user separation as requiring this project's live, in-process Task-tool subagent delegation to become out-of-process subprocess invocation for all seven roles — the largest architecture change on the table, with new operational surface (sudo config, file-ownership engineering) of its own. The lighter bubblewrap option leaves a separate, disclosed gap (database writes via `opsdb.py` are not blocked by filesystem sandboxing alone) needing its own control, and has an open kernel-feasibility question needing a spike before commitment. `risks.id=3` stays `open` through this transition.
Agent recommending it: CTO's original investigation named both candidate shapes (`ops/reviews/cto-risk3-architecture-investigation.md`); the Chief of Staff synthesis framed the choice (`ops/reviews/chief-of-staff-task17-hook-gap-synthesis.md`); the decision itself is the Founder's.
Founder approval: **This decision IS the Founder's own choice** ("Go for option 2 then? Take red team approval", 2026-09-01), made after a full cost/effort/residual-risk walkthrough of both options in this conversation. Recorded as decision id=14 in the operational database. TASK-023 begins immediately at CTO architecture; TASK-017 moves `FOUNDER_APPROVAL` → `BLOCKED`.

### DEC-012 — Finish the risks.id=3 security lock (TASK-023) before any Phase 3 automation work begins
Date: 2026-09-01
Problem: The Founder asked to complete Phase 3 automation, then asked whether everything could be marked 100% complete. Two facts made "yes" impossible to say honestly: (a) the remaining Phase 3 automation — automatic Code Review PASS → QA, QA → Security, Security → Release, and automated Developer reinvocation — has **zero code written**, so "complete" would be false; (b) TASK-023, the security lock DEC-010 and DEC-011 placed in front of that automation, had just **FAILED QA** with four blocking defects, two of which falsified the design's own threat-model claims — the operational database materialising inside every sandbox via `git worktree`, which let QA forge a `review_results` row claiming a **red-team PASS** and flip `risks.id=3` to `resolved`.
Options considered: Drive straight through — finish TASK-023 and build Phase 3 automation in the same push (offered, not chosen); finish TASK-023 to genuinely done, mark that complete, and treat the remaining Phase 3 automation as its own separate Founder decision afterwards (adopted); stop now and record Phase 3 as partially complete with an accurate breakdown (offered, not chosen).
Decision: Finish TASK-023 through its remaining gates — Red Team on addendum 3, Development, Code Review, a QA re-run against the same charter that caught D1–D4, focused Security review, and CTO final conformance. Phase 3 automation is **not** started as part of this; it returns to the Founder as a separate decision once the lock is installed and demonstrably running. Phase 3 stays honestly recorded as `in_progress`: Phase 3A complete (2 of 2), the security lock in progress, the remaining orchestration automation not started.
Reason: The Founder's own instruction was "finish the lock." Independently, the orchestrator had recommended the same thing before the choice was made: every serious defect in this milestone was caught by a human-triggered stage doing real work, and Phase 3 automation is precisely what removes the human trigger. Installing and proving the lock before automating on top of it is the sequencing DEC-008 and DEC-010 already established, and QA's forged-review-row demonstration is concrete evidence of what unattended operation would inherit if the lock shipped unverified. Marking Phase 3 100% complete was refused as a factual misstatement — of exactly the kind this milestone has repeatedly caught: the sandbox that reported `success` while every tool call was denied, the docs claiming a protection that could not fire, and QA's own forged approval record.
Tradeoffs: Phase 3 automation is deferred again, with no date. The remaining TASK-023 work is estimated at 3–8 active hours and that estimate is explicitly **low-confidence** — an earlier estimate of 45–70 minutes for the same remaining gates was immediately followed by a QA FAIL with four blocking defects. Two closing steps are also not the orchestrator's to perform, and were disclosed as such before this decision: a human must run the OS-provisioning runbook (account, sudoers line, gateway and broker services), and a real-endpoint cutover smoke test against the live model service has been deliberately deferred throughout to avoid handling real credential material. **"Lock complete" will therefore mean "built, reviewed and verified up to the provisioning line" — not "installed and live" — and must be reported that way.**
Agent recommending it: Orchestrator/Chief of Staff (recommendation given before the Founder chose, and matching the choice); QA's FAIL (`ops/reviews/qa-task023.md`) is the evidence base.
Founder approval: **This decision IS the Founder's own choice** ("Yeah finish the lock", 2026-09-01), made after being shown the three options and the honest reason Phase 3 could not be marked complete. Recorded as decision id=15 in the operational database.

### DEC-013 — Idea intake becomes a two-gate journey: raw idea → AI-interpreted brief → Founder-approved brief → execution
Date: 2026-09-01
Problem: The Founder observed that the pipeline begins **implementing** an idea without ever confirming it understood the idea correctly. As designed, Start would dispatch Product and then flow straight into Design, CTO, Red Team and Development from the company's *first* interpretation of a one-line idea. A misread idea would not surface until hours of work had been built on it, and the Founder would have had no opportunity to correct the interpretation. Separately, the company has no divergent-thinking step anywhere: Design produces 2–3 concepts, but only about visuals, after the shape is already fixed.
Options considered: Ship the Save/Start front door as designed and add an understanding checkpoint as a later milestone (offered, not chosen); build the understanding checkpoint first and the front door after (offered, not chosen); fold the understanding/brainstorming stage into TASK-024 now, so the front door ships with the checkpoint that makes it safe to press (adopted — the Founder's explicit choice, with a correction to what Start does).
Decision: The intended journey is now **Raw Idea → Save → Refine/Brainstorm → Interpreted Brief → Founder Approval → Start Execution → Design / CTO / Red Team / Development.** "Start work on this" must **not** immediately launch Design, CTO or Development. It first runs an idea-understanding/brainstorming stage which must: use the brainstorming skill where useful, understand the real problem and intended outcome, identify assumptions, explore a few reasonable approaches, recommend a direction, ask only **material** clarifying questions, and produce a clean Product/project brief. That brief is shown to the Founder as *"Here is what I understood you want to build"*, and the Founder must be able to review, correct, refine, or brainstorm further. Only after the Founder approves the interpreted brief does the downstream company workflow begin. All three artifacts must be preserved and distinguishable: **the raw Founder idea, the AI-interpreted brief, and the Founder-approved brief.** The Founder-approved brief — not the raw idea — is the authoritative input to every downstream agent.
Reason: A Start button that reliably begins building the wrong thing is worse than no Start button. The checkpoint is what makes Start safe to press, so shipping them separately would hand the Founder a way to reliably start the wrong work. The Founder was explicit that a raw, possibly incomplete or informal idea must not be treated as the final implementation prompt. Preserving all three artifacts keeps the record honest about what the Founder actually said versus what the company decided it meant — the same discipline this project applies everywhere else.
Tradeoffs: TASK-024 grows materially — a new pipeline stage, new persisted state for the interpreted and approved briefs, new screens for reviewing/correcting/refining an interpretation, and a changed Start route. Product must re-brief and Design must revise artboards already produced (Concept A's Save half should largely survive; the Start flow needs rework). TASK-025 (divergent-thinking stage) is substantially absorbed by this decision — what remains distinct is its first job, verifying whether the `prompt-master` skill referenced in `product.md` actually fires in this deployment, which this milestone now **depends on** and must answer rather than assume. The refinement stage itself spends real model money before any building begins, which must be disclosed honestly on the same screen that already discloses there is no cost estimate available and no stop button.
Agent recommending it: Founder-directed. The orchestrator had recommended folding the checkpoint in (option 1); the correction to what Start does — that it runs refinement rather than execution — is the Founder's own, as is the three-artifact preservation requirement.
Founder approval: **This decision IS the Founder's own directive**, 2026-09-01, given in full as a written specification of the intended journey. Recorded as decision id=17 in the operational database.

### DEC-014 — Idea deciphering is multi-agent, synthesised by Chief of Staff into one Founder-facing answer
Date: 2026-09-01
Problem: DEC-013 established that a raw Founder idea must be interpreted and approved before execution, but left the deciphering stage under-specified — it read as Product alone producing a brief. The Founder has now specified the stage properly. The underlying principle, in the Founder's words: the Founder should **not** be expected to provide a perfect prompt or product specification, and may type something "short, messy, incomplete, uncertain, or conversational." **Raw Founder idea ≠ final implementation prompt.**
Options considered: Product alone interprets the idea and writes the brief (DEC-013's under-specified reading, superseded); every agent reviews every idea (rejected by the directive — *"Do NOT automatically use every agent"*); Product leads, Chief of Staff selects only the additional perspectives that materially improve understanding for **this** idea, agents debate internally, and Chief of Staff synthesises one Founder-facing answer (adopted).
Decision: Deciphering is **multi-agent with Product as lead**. Chief of Staff selects additional participants per idea from CEO, CTO, Red Team, Design (when UX matters), Security/Privacy (only when genuinely relevant) and Financial (only when cost/pricing/viability materially affects the idea) — choosing only perspectives that materially improve understanding, never all of them by default. **Decipher broadly, communicate narrowly:** agents may debate internally, but the Founder never receives separate Product/CEO/CTO/Red Team reports. Chief of Staff returns **one** synthesis with a fixed shape — *What I think you mean / What you are really trying to achieve / My recommended direction / Important alternatives / Assumptions we are making / What I need from you.* Only questions that can materially change direction are put to the Founder; anything agents can reasonably decide themselves, they decide. The interpreted brief has a specified field set (name, original idea, problem/opportunity, goal, target user, desired experience, recommended initial scope, core requirements, constraints, assumptions, alternatives considered, out of scope, risks/uncertainties, open Founder decisions, success criteria), plain language first. The Founder review gate offers **Brainstorm More / Refine / Edit / Approve Brief**, and downstream execution is blocked until approval. Starting work is a **separate consequential action — "Approve Brief & Start Work"** — which must state before the click that agents will begin, real AI cost may be incurred, and the approved brief becomes the authoritative project instruction. Three versions are preserved separately and never overwritten: **raw Founder idea → AI-interpreted brief → Founder-approved brief.** The approved brief — never the raw idea — goes downstream as the implementation prompt.
Reason: This is how a real software company handles a Founder saying *"I have this idea"*: the leaders think about it internally and one person comes back with what we believe you mean, what you are really trying to achieve, what we recommend, the assumptions we made, and the one decision we still need. The Founder corrects or approves, and only then does the company execute. It also fixes the specific failure DEC-013 identified — the company beginning to implement its own first reading of an idea — while avoiding the opposite failure of dumping four agents' reports on the Founder to read.
Tradeoffs: The deciphering stage costs real model money across several agents **before any building begins**, and must be disclosed on the same screen that already discloses no cost estimate is available and there is no stop button. The Chief of Staff's agent-selection judgement becomes load-bearing and is not itself reviewed by anyone. This is a Founder-facing UX change and **not only a workflow change**: the whole journey must be visually coherent and walkable, and Design is explicitly not complete merely because individual screens look attractive. It does **not** authorize unattended automatic pipeline execution — the Founder remains the authority who deliberately starts work. It also depends on the brainstorming-skill question TASK-025 exists to answer and which is still unanswered: the skill referenced in `product.md` may not fire in this deployment at all.
Agent recommending it: Founder-directed, in full, as a written specification. Reproduced verbatim and unedited at `ops/reviews/founder-directive-task024-deciphering.md`, which governs over this summary wherever they differ.
Founder approval: **This decision IS the Founder's own directive**, 2026-09-01. Recorded as decision id=18 in the operational database. Process it sets: Product revises the brief first, then Design revises the mockup; implementation does not proceed until the deciphering + approval UX is visually coherent and Founder-approved.

### DEC-015 — Idea deciphering v2: interpret AND evaluate, no brainstorming concept, approved brief is the downstream source of truth
Date: 2026-09-01
Problem: DEC-014 specified the deciphering stage around a "brainstorming" concept — a *Brainstorm More* action, a dependency on an "existing brainstorming skill" — and Product then established empirically that no such skill exists, that agent `Skill` grants are inert on the runtime path (`--tools ""`), and that divergent thinking is behavioural rather than tooling. The Founder has now simplified and strengthened the stage: the factory's job is to return a high-quality, structured **interpretation and evaluation** of a raw idea before any build work, not to run an ideation loop.
Options considered: Keep DEC-014's brainstorming framing and paper over the missing skill (rejected by the directive — *do NOT build a brainstorming feature, do NOT depend on a brainstorming skill, do NOT make "Brainstorm More" a primary product concept*); treat every idea as a full due-diligence exercise (rejected — depth must scale with the idea); interpret **and** evaluate, synthesised by Chief of Staff into one concise Founder-facing recommendation with progressive disclosure, reviewed via *Edit/Correct · Reconsider · Approve Brief*, with the approved brief becoming the hard downstream source of truth (adopted).
Decision: Journey: **Raw Idea → Factory Interprets → Factory Evaluates → Founder Reviews → Founder Corrects or Approves → Founder-Approved Product Brief → Start Work → Downstream Execution.** Deciphering answers, where relevant: what the Founder appears to mean, the problem/opportunity behind it, the outcome actually sought, who would use it, the value, what should probably be built and what belongs in the first useful version, the important assumptions, whether materially better or simpler approaches exist, whether something similar already exists, known competitors and substitutes, threats, what makes it valuable, and what genuinely requires a Founder decision — explicitly **not** merely rewriting the Founder's sentence. Chief of Staff consults only perspectives that materially improve the interpretation (Product, CEO, CTO, Red Team; Design when UX materially affects the idea; Financial when relevant; Security/Privacy only when genuinely relevant), never all by default, and **depth scales with the idea** — an internal utility needs interpretation, scope, assumptions and a recommendation with almost no competitor research; a commercial SaaS product needs competitors, substitutes, differentiation, threats and cost. Agents may disagree internally; the Founder receives **one** synthesised result. The primary view is a concise executive summary answering ten questions (*understood? really trying to achieve? why worth building? what exists? what could make ours different? what could make it fail? what does the company recommend? what assumptions? what decisions are needed from me? how will we know we succeeded?*) with deeper sections expanding on demand — never a giant consulting report. Founder questions are limited to those materially affecting direction, market, major scope, business intent, meaningful UX direction, irreversible choices, significant cost, external commitments or monetisation; **zero questions is acceptable, and one or two beat eight.** Review actions: **Edit/Correct · Reconsider · Approve Brief** — *Reconsider* means the Founder gives feedback and the company re-evaluates; the objective is **correct understanding, not endless ideation.** Three artifacts preserved separately and never overwritten: raw Founder idea → factory-interpreted/evaluated brief → Founder-approved product brief. **Hard requirement:** once an approved brief exists, every downstream agent receives it as the authoritative instruction; the raw idea is historical context only, and no agent independently reinterprets the one-line idea. Starting execution is a separate deliberate action — *Approve Brief & Start Work* — disclosing before the click that agents will begin, real cost may be incurred, and the approved brief becomes authoritative. Never auto-start from a saved raw idea. Explicitly not built: a brainstorming subsystem, a brainstorming skill dependency, automatic involvement of every agent, agent-report dumps, generic discovery questions, PRD-writing by the Founder, invented competitor information, fake market numbers, fake progress, endless analysis loops.
Reason: In the Founder's words, the factory's job is not merely to say *"I can build what you typed."* It should be able to say: *here is what we believe you mean, what you are really trying to achieve, why it may be worth building, what already exists, what could make ours different, what could make it fail, what we recommend, the assumptions we made, what we genuinely need from you, and how we will know we succeeded* — and the Founder approves that understanding before the company builds. Removing the brainstorming concept aligns the directive with what Product found empirically and keeps the stage on correct understanding rather than ideation.
Tradeoffs: Product's DEC-014 brief (Revision 3) must be revised again, and Design's TASK-024 work must be revised to the full journey — both in the Founder-specified order (Product first, then Design, then show the Founder the clickable mockup before any Development). Evaluation adds competitor and threat analysis under an explicit ban on invented competitors or fake numbers, **with no external research capability wired into any agent today** — how competitor claims are sourced and evidenced is an open question Product must resolve rather than assume. Depth scaling makes Chief of Staff's judgement load-bearing twice over (which perspectives, and how deep). The downstream source-of-truth rule directly implicates the two call sites Product already flagged that feed `business_goal` — the raw idea — into agent transcripts. **Part 2 of 3 of the directive was not received**; Parts 1 and 3 are coherent and actionable, and Part 2 governs over any inference if it arrives.
Agent recommending it: Founder-directed, in full. Reproduced verbatim and unedited at `ops/reviews/founder-directive-task024-deciphering-v2.md`, which governs over this summary wherever they differ.
Founder approval: **This decision IS the Founder's own directive**, 2026-09-01. Recorded as decision id=19 in the operational database.
Addendum (same day): **Part 2 of 3 arrived** after this entry was written and is now inserted verbatim in the directive file. It is the fifteen-section output contract. It resolves the open tradeoff above on competitor sourcing directly: where current competitor information materially affects the decision, use available public research capabilities and never rely only on stale model memory; label every claim **VERIFIED / CURRENT**, **COMPANY INFERENCE** or **UNKNOWN**; never invent pricing, customer counts, revenue, funding, market share or features; and **if research has not been performed, say so.** It also fixes the closing **COMPANY VIEW** (Opportunity High/Medium/Low/Unclear; Recommendation Proceed / Proceed with narrowed scope / Investigate first / Reconsider — executive judgments, not scores). Product's in-flight Revision 4 was told immediately.

### DEC-016 — Founder approves the TASK-024 idea-deciphering journey UX; intake becomes its own section; first live test is the Founder's own dashboard idea
Date: 2026-09-02
Problem: DEC-015 Part 3 §11 required the Founder to see and approve the revised visual/clickable journey before any Development. The Founder rejected a design canvas as the review medium (*"I need UI so I can see clearly"*), so the journey was built into the real Control Center as a **UI-only shell** — the 14 design artboards translated faithfully, zero backend, zero writes (proven across two full walks) — run locally by the Founder on their own machine and walked stage by stage.
Options considered: Build the journey as a second, separate application (rejected — a second app means a second login and a second path to the database, quietly undoing the security posture); keep it buried as one more tab in the 13-tab Control Center (rejected — the Founder wants it to feel separate); the journey becomes **its own section of the same app** — own entry point, own clean chrome, the 13 tabs hidden while inside the flow — with the same login, the same database, and `opsdb.py` as sole writer (adopted).
Decision: **APPROVED** — verbatim *"approve"*, 2026-09-02, after the Founder walked the shell at `http://127.0.0.1:8420/ideas-preview/stage-1.html` on their own machine. The approved rendering is the shipped shell (`ops/control-center/generate_ideas_preview.py`, from `ops/mockups/task024/` Revision 2) and is the visual spec Development builds to. Scope decisions the Founder made in the same exchange: (1) the idea journey ships as its own section inside the same app — **not a second app**; (2) the Founder wants **only this idea capability made real and tested for now**; the child-product build phase follows once it passes, and **TASK-026 and TASK-023 stay parked** until then; (3) the **first live test is the Founder's own dashboard idea** (TASK-026's raw text, already the shell's sample) — passing it both proves the intake and produces the approved brief TASK-026 needs; (4) **"testing done" means, concretely:** a raw idea goes in; the right agents actually run; one synthesis returns with the ten questions answered; the Founder corrects once and it re-evaluates; the Founder approves; a Founder-approved brief exists; and a downstream agent provably receives *that brief*, not the raw sentence (Product's SOT-1). TASK-024 advances `MOCKUP_REVIEW → ARCHITECTURE`.
Reason: The front door is not a detour before building child products — per DEC-015 nothing downstream starts without an approved brief, so making the intake real first is the only coherent order. It also gives the factory its first real end-to-end run, which this project has never had. Staying inside the same app preserves the one login and the single database writer the whole security posture rests on.
Tradeoffs: First time the factory does the real thing end to end — real agent dispatch triggered from the web, real cost, and the first work-creating write route — so expect review rounds; today's pattern is that things that looked finished were not until something ran. The Founder approved the shell knowing no browser existed where it was built and that their walk was its first rendered look; visual defects found later are fixes, not re-approvals. TASK-026 and TASK-023 are parked, not abandoned. Competitor evaluation ships with no research capability, so every such claim is COMPANY INFERENCE or UNKNOWN with "research not performed" stated, per DEC-015 §6.
Agent recommending it: Design (the journey), Developer (the shell), orchestrator (the same-app recommendation and the first-live-test suggestion); the approval and the scope decisions are the Founder's.
Founder approval: **APPROVED 2026-09-02 — the Founder's own word, "approve", after walking the real shell.** Recorded as decision id=21 in the operational database.

### DEC-017 — Founder redirect: a standalone idea-evaluation mockup first; the real backend build is withdrawn until the Founder likes the mockup
Date: 2026-09-02
Problem: DEC-016 recorded the Founder's *"approve"* as approval to build the real deciphering backend, and a CTO architecture agent was dispatched. The Founder corrected this the same day: *"No this is not factory's end to end. I'm not sure what you mean. I need another ui mockup just for idea evaluation. I told you this should be like a stand alone for now if I like it we will add step by step."* The in-app shell, behind the Control Center login and its navigation, read to the Founder as factory wiring rather than as the standalone thing they asked for.
Options considered: Continue the real build under DEC-016 (rejected — the Founder explicitly did not want it); present the in-app `ideas-preview` shell as the mockup (rejected — it lives behind the Control Center login and nav, which is exactly what read as "factory"); one self-contained HTML file that opens by double-click, no server, no login, canned evaluations, clearly labelled as a mockup, with nothing connected to the factory (adopted).
Decision: The CTO architecture agent for the real TASK-024 build is **stopped** and its partial output discarded, not committed. **TASK-024 returns from ARCHITECTURE to MOCKUP_REVIEW.** The deliverable is `ops/mockups/idea-evaluation/index.html` — a standalone, clickable idea-evaluation mockup covering: write the idea in your own words → the company considers it → ten concise answers plus the Company View, with full depth expandable per question → **Edit my idea / Reconsider / Approve brief** → the approved brief with the three stored artifacts listed. **Start work** is present but deliberately not connected, and says so. It is preloaded with the Founder's own dashboard idea at round 2 (round 1 misread the subject, the Founder's real correction is stored beside it, round 2 corrected) so the Reconsider loop is visible without typing anything. New ideas receive honest sample text labelled as sample. The page is also published as a private artifact link for the Founder. Features get added step by step only after the Founder says they like it.
Reason: The Founder's words govern. A standalone page the Founder can judge on its own, with nothing else in view, is what was asked for; the in-app shell and the backend build were the orchestrator's inference, and it was wrong.
Tradeoffs: DEC-016's scope statements (TASK-026 and TASK-023 parked; first live test is the Founder's own idea; SOT-1 as the definition of done) still stand as the eventual plan but are **not in motion**. The mockup's evaluations are canned: it proves the shape of the experience, not that the agents can produce it. Ideas typed into the mockup are kept in the Founder's browser only.
Agent recommending it: Founder-directed. Recorded by orchestrator.
Founder approval: **This decision IS the Founder's own redirect**, 2026-09-02. Recorded as decision id=22 in the operational database.
